language: python
python:
    - 3.6
sudo: required
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - g++-4.9
env:
  - CC=gcc-4.9 CXX=g++-4.9
script:
  - install -d build
  - cd build
  - cmake ..
  - make VERBOSE=1
  - make test ARGS='-V'
